# Credentials для sync MinIO local → VPS (НЕ коммитить)
# Скопируйте в vps.minio.env и подставьте значения из prod.env на сервере

VPS_MINIO_ENDPOINT=https://tsehh.ru:9000
VPS_MINIO_ACCESS_KEY=
VPS_MINIO_SECRET_KEY=
VPS_MINIO_BUCKET=antiquar-products
